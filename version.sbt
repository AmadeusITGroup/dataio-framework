// Plain semver only. The Spark line is appended per profile at publish time, so that a single
// checkout can publish every supported Spark line. See SparkProfile.publishedVersion.
ThisBuild / version := "1.1.1-SNAPSHOT"
