# This file should stay at the root of the resources directory, as it is where typesafe looks for a default
# application.conf file to load.

input {
    type = "com.DefaultConfigInput"
}

processing {
    type = "com.DefaultConfigProcessor"
}

output {
    type = "com.DefaultConfigOutput"
}
